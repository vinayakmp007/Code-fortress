CREATE TABLE login
(
id int NOT NULL AUTO_INCREMENT,
tname varchar(255) NOT NULL,
cname varchar(255) NOT NULL,
email varchar(40),
mno varchar(12),
pass varchar(12),
lang varchar(10),
PRIMARY KEY (ID)
)
